# SparkChat 需求与验收规范

## 1. 产品定位

SparkChat 是面向手机 WebView 的变形金刚主题数字角色对话应用。用户先登录并选择角色，再进入文本聊天或实时语音通话。界面应保持清晰、整齐、美观，适合移动端连续使用。

## 2. 账号与会话

- 支持账号密码注册、登录和退出。
- 预置账号：`CaraLin`，密码：`2766`。
- 登录状态通过长期 Cookie 保存；生产环境必须固定 `FLASK_SECRET_KEY`。
- 角色、消息和自定义音色按用户隔离。
- 退出后受保护 API 必须返回未登录错误。

## 3. 角色配置

全局角色配置和新建角色统一只暴露以下三项：

1. 角色名称：最多 40 个字符。
2. 身份背景：描述角色身份、经历、性格、价值观和表达方式，最多 2400 个字符。
3. 角色音色：从音色库选择，或通过 ElevenLabs Voice Design 创建。

旧版本的“一句话定位”“背景”“初始记忆”等字段不再作为用户可配置项，也不应进入最终智能体提示词。数据库旧列可以暂时保留用于迁移兼容，但新界面和提示词构造不得依赖这些字段。

预置角色包含威震天及若干音色。威震天身份背景以跨作品共同设定和 IDW 2005 主宇宙经历为基础，不将对话规则、思考模式开关或系统控制语句写入身份背景。

## 4. 提示词构造

最终发送给大语言模型的内容必须满足：

```text
用户提示词 = 角色名称 + 身份背景 + 角色音色相关上下文（如调用链路需要）
系统提示词 = 通用对话规则
最终提示词 = 用户提示词 + 系统提示词
```

通用规则包括中文表达、回答长度、角色一致性、知识边界和安全边界。系统规则不得混入角色配置界面或角色身份背景。

关闭大语言模型思考模式属于模型调用参数，由后端调用 API 时传入 `thinking.type=disabled`；提示词中不得出现“关闭思考”“不展示思考过程”等控制内容。

## 5. 对话功能

- 联系人首页显示角色头像、名称、最近消息、时间和未读数量。
- 聊天页不显示底部导航，提供返回、角色配置和语音通话入口。
- 用户消息保留气泡样式，但不显示“你”“You”等身份标签。
- 支持文本流式回复、语音转文字、角色回复朗读。
- 多行文本输入框在内容过长时显示浏览器原生垂直拖拽手柄，用户可以拉高输入框查看内容。
- 聊天发送框保持稳定高度，不因拖拽破坏底部操作区。

## 6. 语音能力

### 6.1 文本朗读

- 使用 ElevenLabs TTS，而不是浏览器系统音色。
- 朗读前清理动作、音效、括号和星号舞台提示。
- 音色额度不足、未配置或上游不可用时返回明确的 503 错误，前端显示可理解的失败原因。

### 6.2 实时语音通话

- 通过 ElevenLabs Conversation WebRTC token 建立通话。
- 通话页显示角色、连接状态、字幕、麦克风静音和挂断控制。
- Speech Engine 使用流式模型回复。
- 当前 Speech Engine 由单个 `SPEECH_ENGINE_ID` 驱动，令牌接口只对已绑定实时引擎的预置角色开放；自定义角色需要独立引擎映射后才能开放实时通话。
- 实时语音的角色身份应通过服务端 Speech Engine 配置维护，不得在客户端暴露 API 密钥。

## 7. 页面与视觉要求

- 顶部无无功能 Logo 顶栏，首页保留联系人、新建角色、我的底部导航。
- 我的页面中用户名和退出按钮必须有明确间距，不得挤在一起。
- 页面在移动端和桌面窄屏均不得横向溢出或出现文字重叠。
- 控件名称、状态反馈和错误提示使用简洁中文；图标按钮提供无障碍标签。

## 8. 验收测试

### 自动化

```powershell
python -m unittest -v test_app.py
```

必须覆盖：登录、预置角色、会话 Cookie、角色私有性、角色编辑、预置角色覆盖、退出、提示词构造和自定义角色实时引擎隔离。

### 前端

- 检查联系人、新建角色、角色聊天、角色配置和我的页面。
- 新建和编辑页面字段必须恰好为角色名称、身份背景、角色音色。
- 新建页面不得存在 `input[type=range]`。
- 用户消息不得出现 `You` 或其他身份标签。
- 多行文本框计算样式必须为 `resize: vertical`，聊天发送框除外。
- 检查桌面和移动视口无横向溢出。

### 真实服务

- 文本流式对话：验证上游 Ark 可用并确认 SSE 增量返回。
- TTS：验证 ElevenLabs 返回音频；额度不足时确认返回 503 而非 500。
- 实时通话：验证 token 返回、WebRTC 连接、字幕、静音、挂断和 Speech Engine 流式回复。
- 公网环境：部署后访问 `https://visionvoice.cn/sparkchat/`，并分别检查网页、API、TTS 和实时语音。

## 9. 部署配置

生产环境至少配置：

```dotenv
ELEVENLABS_API_KEY=...
ARK_API_KEY=...
FLASK_SECRET_KEY=...
SPEECH_ENGINE_ID=...
SPEECH_ENGINE_PORT=3101
SPARKCHAT_VOICE_MEGADEEP=...
SPARKCHAT_VOICE_IRONVOW=...
SPARKCHAT_VOICE_STARLIGHT=...
SPARKCHAT_VOICE_ARCHIVE=...
```

Web 服务和 Speech Engine 必须分别启动，Nginx 将公网 `/sparkchat/ws` 转发到 Speech Engine。部署同步应通过 Git 远端或服务器上的受控发布流程完成，并在服务器上重新执行自动化测试和真实语音验收。